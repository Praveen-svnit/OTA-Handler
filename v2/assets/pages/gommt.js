/**
 * GoMMT — already defined inside booking.js as window.PAGE_GOMMT.
 * This file exists just so index.html's static script tag has something to load
 * and so the file structure is obvious. Booking and GoMMT share the same render
 * function, driven by the CFG object inside booking.js.
 */
